const PageTitle = ({ showSidebar, setShowSidebar }) => (
  <div className="flex items-center gap-2 text-primary dark:text-primary-dark">
    <p>
      <a href="#">
        <span className="text-lg font-sembold">Amr Abed</span>
      </a>
    </p>
  </div>
);

export default PageTitle;
