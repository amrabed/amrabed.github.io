import { Role } from "@/types";

const roles: Record<string, Role> = {
  engineer: {
    color: "blue",
  },
  researcher: {
    color: "orange",
  },
  instructor: {
    color: "green",
  },
};

export default roles;
